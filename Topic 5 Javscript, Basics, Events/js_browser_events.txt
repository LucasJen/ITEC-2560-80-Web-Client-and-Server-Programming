What are JS/browser events? explain the syntax for event handlers

Common Events (generated by Claude)

Mouse: click, dblclick, mousedown, mouseup, mousemove, mouseenter, mouseleave
Keyboard: keydown, keyup, keypress
Form: submit, change, input, focus, blur
Window: load, resize, scroll
Touch: touchstart, touchmove, touchend


What are JS/browser events


the event loop is how javascript handles asynchronous operations, like user interaction.

The event loop is ran in the browser's underlying code where it constantly monitors the page for 
interaction

If the event occurs, the JavaScript is notified to run the function the corresponds to the trigger.

element.addEventListener(click, function() {
    alert('hello')
})

the event handler code runs when its specific even is triggered 